---
id: ports
title: Ports
sidebar_label: Ports
sidebar_position: 3
slug: /terminal-staff/ports
description: Manage ports — create, edit, and delete the terminals that routes connect and that host gates and berths.
keywords:
  - ports
  - port
  - terminal
  - timezone
  - master data
---

A **port** is a terminal that ferries depart from and arrive at. Ports are the
origin and destination of [routes](/terminal-staff/routes) and host the terminal's
[gates](/terminal-staff/gates) and [berths](/terminal-staff/berths). Each port has a
**timezone**, which drives the local times shown across the system.

Ports are the **first thing to create** when setting up a new terminal — see
[How the Pieces Fit Together](/how-it-fits-together) for the full order.

> **Ports → Create Port / Actions (Edit · Delete)**

:::note
You need permission to manage ports. The screenshots below use
`https://ts-terminal.sindoferry.com.sg/`.
:::

## The Ports list

Open **Ports** (route: `/ports`). Each row shows the port's **Code**, **Name**, and
**Timezone**, plus an **Actions** (**⋯**) menu. Use the **search** box to find a
port.

![The Ports list view](/img/terminal-staff/ports/01-ports-list.png)
_The Ports list — Code, Name, and Timezone._

## Create a port

Click **Create Port** (top right) and fill in the **New Port** form:

| Field | Notes |
| --- | --- |
| **Port code** | A short unique code, e.g. `PRT-01`. |
| **Name** | The port's name, e.g. `Waterfront`. |
| **Timezone** | Type the port's **IANA timezone ID**, e.g. `Asia/Singapore` — this drives the local times shown for its trips and on the public boards. |

:::note[Timezone is typed, not picked from a list]
**Timezone** is a **free-text** field, so any valid IANA zone can be used — you are
not limited to a fixed list. Enter the ID in `Region/City` form, such as
`Asia/Singapore` or `Asia/Jakarta`.

The server checks the value when you save. An unrecognised entry is rejected with
_"Timezone '&lt;value&gt;' is not a valid IANA timezone ID. Use a value like
'Asia/Singapore'."_
:::

Then click **Create Port**.

![The New Port form, with Port code WFC and name Waterfront Terminal](/img/terminal-staff/ports/02-create-port.png)
_Creating a port — **WFC / Waterfront Terminal**, with the Timezone field ready for an IANA ID._

:::warning[Unique code]
The **Port code** must be **unique** across all ports.
:::

## Edit and delete

From the row **⋯ Actions** menu you can **Edit** the port's code, name, or timezone,
or **Delete** it (confirmation required).

:::info[Delete is blocked when in use]
A port **cannot be deleted** while it still has **berths** attached or is **used by
routes**. Remove or reassign those first.
:::

## Next steps

- [Gates](/terminal-staff/gates)
- [Berths](/terminal-staff/berths)
- [Routes](/terminal-staff/routes)

---
id: operators
title: Operators
sidebar_label: Operators
sidebar_position: 1
slug: /terminal-staff/operators
description: Manage ferry operators — create, edit, activate/deactivate, and delete operator records used across vessels, routes, timeslots, and trips.
keywords:
  - operators
  - operator
  - master data
  - activate
  - deactivate
---

An **operator** is a ferry company that runs services from the terminal. Operators
are referenced throughout the system — by [vessels](/terminal-staff/vessels),
[routes](/terminal-staff/routes), [timeslots](/terminal-staff/timeslots), and trips.

> **Operators → Create Operator / Actions (Edit · Activate · Delete)**

:::note
You need permission to manage operators. The screenshots below use
`https://ts-terminal.sindoferry.com.sg/`.
:::

## The Operators list

Open **Operators** (route: `/operators`). Each row shows the operator's **Code**,
**Name**, **Trip Code**, and **Status** (**Active** / **Inactive**), plus an
**Actions** (**⋯**) menu. Use the **search** box and **Show Filters** (Status) to
narrow the list.

![The Operators list view](/img/terminal-staff/operators/01-operators-list.png)
_The Operators list — Code, Name, Trip Code, and Status._

## Create an operator

Click **Create Operator** (top right) to open the **New Operator** form. It has two
parts: the **operator details** and the operator's **Admin Account**.

### 1. Operator details

| Field | Notes |
| --- | --- |
| **Operator code** | A short unique code, e.g. `OP001`. |
| **Trip code** | A short code used when generating trip codes. |
| **Name** | The operator's name. |
| **Logo URL** | A link to the operator's logo (`https://…`), shown on the public boards. Must be a valid URL. |

### 2. Admin Account

Below the operator details, enter the details of the operator's administrator —
the form notes that _"This user will be created with Admin & Super Admin right"_:

| Field | Notes |
| --- | --- |
| **Username** | The administrator's username, e.g. `jane.doe`. |
| **Email** | The administrator's email, e.g. `admin@operator.com`. |

Then click **Create Operator**.

![The New Operator form, with the operator details and the Admin Account section](/img/terminal-staff/operators/02-create-operator.png)
_Creating an operator — operator details plus the Admin Account._

:::info[One super-admin user is created with the operator]
Submitting the form creates the operator **and**, in the same step, **one user** for
that ferry operator — granted **Admin** and **Super Admin** rights and set
**active**. This account is the ferry operator's administrator: they use it to sign
in to the **Operator Portal** and create the operator's other users from there.
:::

:::warning[Validation]
- Both the **Operator code** and the **Trip code** must be **unique** across all
  operators.
- The **Logo URL** must be a valid URL.
- The **Username** is required, and the **Email** must be a valid email address.
:::

## Edit, activate/deactivate, and delete

From the row **⋯ Actions** menu:

- **Edit** — update the operator's code, name, trip code, or logo.
- **Activate / Deactivate** — toggle whether the operator is available for use.
- **Delete** — permanently remove the operator (confirmation required).

![The Actions menu on an operator row](/img/terminal-staff/operators/03-actions.png)
_The Actions menu — Edit, Activate/Deactivate, and Delete._

:::info[Delete is blocked when in use]
An operator **cannot be deleted** while it still has **vessels**, or is assigned to
**timeslots** or **trips** — the system asks you to set the operator **inactive**
instead. Deactivating keeps the history intact while removing it from new selections.
:::

## Next steps

- [Vessels](/terminal-staff/vessels)
- [Routes](/terminal-staff/routes)
